### Verification

Before commiting changes, run:
- `cargo clippy` to ensure code quality and consistency .
- `cargo fmt` to format the code according to Rust's style guidelines.
- `cargo test` to run all tests and ensure that your changes do not break existing functionality.
